<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.origamixr" >

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".StartScreen">
            <intent-filter> <!--Just move the intent filter tag into the main activity tag-->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainHomeActivity">

        </activity>

        <activity android:name=".InstructionActivity" />
        <activity android:name=".InstructionStartActivity" />
        <activity android:name=".InstructionEndActivity" />
        <activity android:name=".LoginScreen"></activity>

    </application>

</manifest>